import { Story, Meta } from '@storybook/react';
import CouponButton from '.';

export default {
  title: 'CouponButton',
  component: CouponButton
} as Meta;

export const Default: Story = () => <CouponButton />;
